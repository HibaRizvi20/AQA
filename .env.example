# AQA demo environment template. Copy to `.env` and fill in your own values.
# No real hosts, credentials, or internal endpoints — this is a generic demo.

# App under test (the fictional "Acme Shop" demo)
APP_BASE_URL=http://localhost:3000
TEST_USERNAME=demo@example.com
TEST_PASSWORD=changeme

# Issue tracker (generic REST API — plug in your own)
TRACKER_BASE_URL=https://tracker.example.com
TRACKER_USER=you
TRACKER_TOKEN=your-token

# Pick a subset of tagged tests
# AQA_TAGS="@area:checkout and @type:happy"
